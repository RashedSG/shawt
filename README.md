# شوط

موقع ويب ثابت (HTML / CSS / JS). لا يحتاج بيلد.

## تشغيل محلي
افتح `index.html` أو:

```
python3 -m http.server 8080
```

## GitHub Pages
Settings → Pages → Deploy from branch → `main` → `/ (root)`

https://rashedsg.github.io/shawt/
